group 'com.holi'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    mavenCentral()
}

apply plugin: 'idea'

idea {
    module {
        excludeDirs += ['.idea', '.gradle'].collect { file(it) };
    }
}